import { Box } from '@mui/material';
import { BrowserRouter, Route, Routes } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
    <Box sx={{ pt: "58px" }}>
    <Routes>
    </Routes>
    </Box>
  </BrowserRouter>
  );
}

export default App;
